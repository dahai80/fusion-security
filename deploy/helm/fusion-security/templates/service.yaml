apiVersion: v1
kind: Service
metadata:
  name: {{ include "fusion-security.fullname" . }}
  labels:
    {{- include "fusion-security.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "fusion-security.selectorLabels" . | nindent 4 }}
