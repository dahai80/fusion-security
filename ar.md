# Fusion Security 与 Fusion 生态各组件的关系详解

Fusion Security 是 Fusion 全栈生态中**安全垂直领域的上层应用产品**，与 fusion-mlx、fusion-agent-studio、fusion-code、fusion-multi-node、fusion-studio 形成清晰的「底层算力→核心底座→垂直应用→分布式扩展→统一入口」层级依赖与协同关系，所有组件共享底层技术栈、避免重复造轮子，同时保持独立的产品边界。

对标 Anthropic 生态：Claude Security 基于 Claude 底座模型 + Agent 框架构建，与 Claude Code 协同、集成在 Claude 官方工作台中；Fusion 生态各组件的分工与协作逻辑完全对齐这一架构，但实现了全栈可私有化、全链路可定制。

---

## 一、各组件与 Fusion Security 的关系逐一拆解

### 1. fusion-mlx：底层算力底座，强依赖关系

#### 组件定位

fusion-mlx 是整个 Fusion 生态的**本地大模型推理加速引擎**，基于 Apple MLX 框架深度封装，提供模型加载、量化加速、推理调度、显存 / 内存优化、多模型统一管理等核心能力，是所有 AI 能力的统一算力入口。

#### 与 Fusion Security 的核心关系

**强依赖关系：Fusion Security 所有 AI 推理能力完全构建在 fusion-mlx 之上**，是 fusion-mlx 在安全领域的上层应用。

#### 具体交互与复用点

- Fusion Security 的语义理解、数据流追踪、漏洞验证、补丁生成等所有核心 AI 能力，全部调用 fusion-mlx 的标准推理接口执行，不自研推理引擎
- 复用 fusion-mlx 的模型管理体系，支持本地模型切换、量化精度调节、推理参数调优，适配不同性能与精度需求
- 完全复用 MLX 的 Apple Silicon 原生加速能力，实现本地私有化部署下的高性能代码扫描，代码数据全程不出内网

#### 对标 Claude 生态

等价于 Anthropic 的 Claude 模型推理引擎层，Claude Security 的所有能力均基于 Claude Opus 模型的底层推理能力支撑。

---

### 2. fusion-agent-studio：核心运行底座，强依赖关系

#### 组件定位

fusion-agent-studio 是 Fusion 生态的**智能体统一开发与运行平台**，提供 Agent SDK、工具调用框架、可视化工作流编排器、多智能体协作机制、记忆管理、任务调度、权限体系等通用能力，是所有 Agent 类产品的公共技术底座。

#### 与 Fusion Security 的核心关系

**强依赖关系：Fusion Security 是基于 fusion-agent-studio 构建的垂直领域安全智能体应用**，属于 Agent Studio 上的一级垂直场景落地产品。

#### 具体交互与复用点

- Fusion Security 的「侦察→发现→验证→分诊→补丁」五阶段标准扫描管线，完全基于 fusion-agent-studio 的工作流编排能力实现，无需自研工作流引擎
- 扫描 Agent、对抗性验证 Agent、补丁生成 Agent 三类核心智能体，均基于 Agent Studio 的 Agent SDK 开发，复用其工具调用、状态管理、上下文传递、异常处理能力
- 复用 Agent Studio 的工具集成框架，可快速接入 Semgrep、CodeQL、Bandit 等第三方安全工具作为前置过滤能力
- 共享 Agent Studio 的任务队列、日志审计、权限管控等基础运维能力

#### 对标 Claude 生态

等价于 Anthropic 内部的 Agent 开发框架 + 安全管线编排系统，对外对应 Anthropic Agent SDK，Claude Security 的底层 Agent 执行逻辑均基于该体系构建。

---

### 3. fusion-code：平级兄弟产品，双向协同关系

#### 组件定位

fusion-code 是 Fusion 生态的**代码研发专属智能体应用**，聚焦代码生成、智能补全、重构、调试、代码评审、技术栈迁移等研发全流程辅助，同样基于 fusion-agent-studio 构建。

#### 与 Fusion Security 的核心关系

**平级并行的兄弟产品，共享底层底座，双向能力复用与场景集成**，不存在谁包含谁的从属关系。二者分别覆盖「研发效率」与「代码安全」两个赛道，共同构成 AI 研发全链路能力。

#### 具体交互与复用点

1. **能力底层复用**：Fusion Security 的补丁生成模块，直接复用 fusion-code 的代码理解、语法校验、项目风格匹配、代码格式化能力，避免重复建设代码生成引擎；二者共享代码 AST 解析、调用图构建、依赖分析等基础预处理能力，同一项目只需一次解析，两端均可复用结果。
2. **产品场景集成**：fusion-code 的 IDE 插件、MR/PR 代码评审功能中，可内嵌调用 Fusion Security 能力，实现「编写代码→实时安全检测→一键生成修复补丁」的研发安全闭环；Fusion Security 的漏洞详情页可跳转至 fusion-code 进行深度代码交互与修复调试。
3. **数据打通**：二者共享项目维度的代码仓库、开发分支、人员权限数据，在同一个项目空间内无缝切换使用。

#### 对标 Claude 生态

等价于 Claude Security 与 Claude Code 的关系：Claude Security 是安全专项能力，Claude Code 是通用编码助手，Claude Security 发现的漏洞会在 Claude Code 中打开供开发者审核修复，二者协同形成闭环Anthropic。

---

### 4. fusion-multi-node：分布式扩展底座，可选依赖关系

#### 组件定位

fusion-multi-node 是 Fusion 生态的**分布式多节点调度与集群管理框架**，负责跨机器的任务分发、资源调度、负载均衡、分布式推理、节点状态监控，支撑生态产品从单机向集群的水平扩展。

#### 与 Fusion Security 的核心关系

**可选增强依赖：单机版 Fusion Security 无需依赖，企业级大规模部署场景下作为扩展底座**。

#### 具体交互与复用点

- 企业级多项目并发扫描、超大规模代码仓库全量扫描场景下，Fusion Security 通过 fusion-multi-node 将扫描任务拆分为子任务，分发到多个工作节点并行执行，大幅提升扫描效率
- 大模型分布式推理场景下，fusion-multi-node 协同 fusion-mlx 实现多机模型分片推理，支撑高并发推理需求
- 复用其节点健康监控、任务失败重试、资源弹性调度、故障自动转移能力

#### 对标 Claude 生态

等价于 Anthropic 云端的分布式算力调度集群，属于后台基础设施，对外不直接暴露，是支撑大规模企业用户的底层能力。

---

### 5. fusion-studio：统一入口门户，集成承载关系

#### 组件定位

fusion-studio 是 Fusion 全生态的**统一前端工作台与用户门户**，整合所有 Fusion 系列产品的交互界面，提供统一账号体系、统一项目管理、统一数据看板、统一权限管控，是用户接触所有 Fusion 产品的唯一入口。

#### 与 Fusion Security 的核心关系

**集成承载关系：Fusion Security 无独立前端产品，其 Web 控制台完全嵌入 fusion-studio 中**，作为工作台的一个核心功能模块存在。

#### 具体交互与复用点

- Fusion Security 的扫描任务管理、漏洞列表、补丁预览、安全报表、系统设置等所有前端页面，均以模块形式集成在 fusion-studio 内
- 复用 fusion-studio 的统一账号、RBAC 权限、组织架构、项目空间、操作审计日志等通用管理能力，无需单独建设用户体系
- 与 fusion-code 及其他 Fusion 应用共享项目维度数据，在同一个项目空间内可同时使用代码研发、安全审计等多项能力

#### 对标 Claude 生态

等价于 claude.ai 官方网页工作台，Claude Security 作为功能模块集成在其中，用户通过统一入口访问所有能力。

---

## 二、生态层级全景架构

从下到上的完整层级与依赖关系如下：

```
┌─────────────────────────────────────────────────────────┐
│         fusion-studio（统一工作台 / 入口层）             │
│  ┌─────────────┐  ┌────────────────┐  ┌─────────────┐   │
│  │ fusion-code │  │ Fusion Security│  │  其他应用   │   │
│  └─────────────┘  └────────────────┘  └─────────────┘   │
├─────────────────────────────────────────────────────────┤
│       fusion-agent-studio（Agent编排 / 核心底座层）     │
├─────────────────────────────────────────────────────────┤
│             fusion-mlx（推理加速 / 算力引擎层）         │
├─────────────────────────────────────────────────────────┤
│     fusion-multi-node（分布式调度 / 基础设施扩展层）    │
└─────────────────────────────────────────────────────────┘
```

---

## 三、关系总览对照表

表格

| 组件名称 | 生态层级 | 核心定位 | 与 Fusion Security 的关系 | 依赖强度 | 对标 Claude 生态对应项 |
| --- | --- | --- | --- | --- | --- |
| fusion-mlx | 算力引擎层 | 本地大模型推理加速底座 | 所有 AI 推理的底层算力支撑 | 强依赖（必须） | Claude 模型推理引擎 |
| fusion-agent-studio | 核心底座层 | 智能体开发与运行平台 | 安全 Agent 的开发与运行底座 | 强依赖（必须） | Anthropic Agent SDK + 内部管线框架 |
| fusion-code | 垂直应用层 | 代码研发智能助手 | 平级兄弟产品，双向能力复用与场景集成 | 协同关系（非必须） | Claude Code |
| fusion-multi-node | 基础设施层 | 分布式多节点调度框架 | 企业级大规模部署的扩展支撑 | 可选依赖 | Anthropic 云端算力集群 |
| fusion-studio | 入口层 | 全生态统一工作台 | 前端界面承载与统一用户体系 | 集成承载 | claude.ai 官方工作台 |

---

需要我补充**Fusion Security 与各组件的核心接口清单**，或者绘制更详细的**跨组件数据流转拓扑图**吗？

需要，整体都写到一篇文档中

已完成思考

# Fusion Security 产品需求文档（PRD）与技术架构方案

**文档版本**：v1.0
**文档状态**：正式初稿
**对标产品**：Claude Security (Anthropic)
**所属生态**：Fusion-Agent Studio v2.0 全栈生态
**适用范围**：产品规划、技术研发、交付部署全流程

---

## 一、产品概述

### 1.1 产品定位

Fusion Security 是一款基于大语言模型的**代码语义级安全审计与自动修复平台**，对标 Claude Security 的核心能力，聚焦 DevSecOps 全流程闭环。它以 Fusion 全栈生态为底座，结合本地 MLX 推理加速引擎，为企业提供可私有化部署、可深度定制、模型可替换的 AI 原生安全解决方案。

与 Claude Security 的纯托管式服务不同，Fusion Security 采用「**开源内核 + 商业增强**」双轨模式，既支持开箱即用的标准化扫描，也允许安全团队基于 Agent SDK 构建专属安全智能体，适配企业独特的安全规范与技术栈。

### 1.2 核心价值主张

- **语义级漏洞发现**：超越传统 SAST 工具的规则匹配，实现跨文件、跨组件的数据流追踪与逻辑漏洞识别
- **从扫描到修复的闭环**：检测 → 验证 → 分诊 → 补丁生成 → 人工审核 → 合并，全链路自动化
- **极低误报率**：内置对抗性自验证机制，每一条漏洞结论都经过反向质询与交叉校验
- **私有化可部署**：完整支持本地模型推理（MLX 加速），代码与敏感数据永不离开企业内网
- **深度可定制**：基于 Fusion-Agent 生态，可灵活接入企业现有 SAST/DAST/IAST 工具链

### 1.3 对标 Claude Security 差异分析

表格

| 维度 | Claude Security | Fusion Security |
| --- | --- | --- |
| 部署模式 | 纯托管 SaaS | 私有化部署 + 云端混合部署 |
| 模型底座 | 锁定 Claude 系列 | 多模型支持 (Qwen/DeepSeek/Llama 等)，原生 MLX 本地加速 |
| 定制能力 | 有限 API 接入 | 全链路可编程，Agent SDK 深度定制 |
| 代码隐私 | 代码上传 Anthropic 服务器 | 本地扫描，支持完全离线模式 |
| 生态集成 | Slack/Jira/GitHub | 兼容 DevOps 全栈，深度融合 Fusion 全生态 |
| 开源程度 | 闭源商业产品 | 核心扫描管线开源，企业版功能增强 |
| 成本模型 | 按企业席位订阅 | 开源免费 + 商业支持订阅 |

---

## 二、Fusion 生态定位与组件关系

Fusion Security 是 Fusion 全栈生态中**安全垂直领域的上层应用产品**，与 fusion-mlx、fusion-agent-studio、fusion-code、fusion-multi-node、fusion-studio 形成清晰的「底层算力→核心底座→垂直应用→分布式扩展→统一入口」层级依赖与协同关系，所有组件共享底层技术栈、避免重复造轮子，同时保持独立的产品边界。

### 2.1 生态整体层级架构

从下到上的完整依赖层级如下，Fusion Security 位于垂直应用层，向下依赖底座能力，向上承载于统一入口。

```
┌─────────────────────────────────────────────────────────────┐
│        fusion-studio（统一工作台 / 入口层）                  │
│  ┌──────────────┐  ┌─────────────────┐  ┌──────────────┐   │
│  │ fusion-code  │  │ Fusion Security │  │  其他应用    │   │
│  └──────────────┘  └─────────────────┘  └──────────────┘   │
├─────────────────────────────────────────────────────────────┤
│      fusion-agent-studio（Agent编排 / 核心底座层）          │
├─────────────────────────────────────────────────────────────┤
│            fusion-mlx（推理加速 / 算力引擎层）              │
├─────────────────────────────────────────────────────────────┤
│    fusion-multi-node（分布式调度 / 基础设施扩展层）         │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 各组件与 Fusion Security 的关系详解

#### 2.2.1 fusion-mlx：底层算力底座（强依赖，必须组件）

- **组件定位**：Fusion 生态的本地大模型推理加速引擎，基于 Apple MLX 框架深度封装，提供模型加载、量化加速、推理调度、显存 / 内存优化、多模型统一管理等核心能力，是所有 AI 能力的统一算力入口。
- **核心关系**：Fusion Security 所有 AI 推理能力完全构建在 fusion-mlx 之上，是 fusion-mlx 在安全领域的上层应用，无独立推理引擎。
- **具体复用点**：
  1. 语义理解、数据流追踪、漏洞验证、补丁生成等所有核心 AI 能力，全部调用 fusion-mlx 标准推理接口执行
  2. 复用模型管理体系，支持本地模型切换、量化精度调节、推理参数调优，适配不同性能与精度需求
  3. 完全复用 MLX 的 Apple Silicon 原生加速能力，实现本地私有化部署下的高性能代码扫描
- **对标 Claude 生态**：等价于 Anthropic 的 Claude 模型推理引擎层，Claude Security 所有能力均基于 Claude 模型的底层推理支撑。

#### 2.2.2 fusion-agent-studio：Agent 运行底座（强依赖，必须组件）

- **组件定位**：Fusion 生态的智能体统一开发与运行平台，提供 Agent SDK、工具调用框架、可视化工作流编排器、多智能体协作机制、记忆管理、任务调度、权限体系等通用能力，是所有 Agent 类产品的公共技术底座。
- **核心关系**：Fusion Security 是基于 fusion-agent-studio 构建的垂直领域安全智能体应用，属于 Agent Studio 上的一级垂直场景落地产品。
- **具体复用点**：
  1. 「侦察→发现→验证→分诊→补丁」五阶段标准扫描管线，完全基于 fusion-agent-studio 的工作流编排能力实现
  2. 扫描 Agent、对抗性验证 Agent、补丁生成 Agent 三类核心智能体，均基于 Agent Studio 的 Agent SDK 开发，复用其工具调用、状态管理、上下文传递、异常处理能力
  3. 复用工具集成框架，可快速接入 Semgrep、CodeQL、Bandit 等第三方安全工具作为前置过滤能力
  4. 共享任务队列、日志审计、权限管控等基础运维能力
- **对标 Claude 生态**：等价于 Anthropic 内部的 Agent 开发框架 + 安全管线编排系统，对外对应 Anthropic Agent SDK。

#### 2.2.3 fusion-code：平级协同产品（双向复用，非必须组件）

- **组件定位**：Fusion 生态的代码研发专属智能体应用，聚焦代码生成、智能补全、重构、调试、代码评审、技术栈迁移等研发全流程辅助，同样基于 fusion-agent-studio 构建。
- **核心关系**：平级并行的兄弟产品，共享底层底座，双向能力复用与场景集成，不存在从属关系。二者分别覆盖「研发效率」与「代码安全」两个赛道，共同构成 AI 研发全链路能力。
- **具体协同点**：
  1. **能力底层复用**：Fusion Security 的补丁生成模块，直接复用 fusion-code 的代码理解、语法校验、项目风格匹配、代码格式化能力；二者共享代码 AST 解析、调用图构建、依赖分析等基础预处理能力，同一项目只需一次解析。
  2. **产品场景集成**：fusion-code 的 IDE 插件、MR/PR 代码评审功能中，可内嵌调用 Fusion Security 能力，实现「编写代码→实时安全检测→一键生成修复补丁」的研发安全闭环；Fusion Security 的漏洞详情页可跳转至 fusion-code 进行深度代码交互与修复调试。
  3. **数据打通**：二者共享项目维度的代码仓库、开发分支、人员权限数据，在同一个项目空间内无缝切换使用。
- **对标 Claude 生态**：等价于 Claude Security 与 Claude Code 的关系，二者协同形成研发安全闭环。

#### 2.2.4 fusion-multi-node：分布式扩展底座（可选依赖，企业级增强）

- **组件定位**：Fusion 生态的分布式多节点调度与集群管理框架，负责跨机器的任务分发、资源调度、负载均衡、分布式推理、节点状态监控，支撑生态产品从单机向集群的水平扩展。
- **核心关系**：可选增强依赖，单机版 Fusion Security 无需依赖，企业级大规模部署场景下作为扩展底座。
- **具体复用点**：
  1. 多项目并发扫描、超大规模代码仓库全量扫描场景下，将扫描任务拆分为子任务，分发到多个工作节点并行执行，大幅提升扫描效率
  2. 大模型分布式推理场景下，协同 fusion-mlx 实现多机模型分片推理，支撑高并发推理需求
  3. 复用节点健康监控、任务失败重试、资源弹性调度、故障自动转移能力
- **对标 Claude 生态**：等价于 Anthropic 云端的分布式算力调度集群，属于后台基础设施。

#### 2.2.5 fusion-studio：统一入口门户（集成承载，前端入口）

- **组件定位**：Fusion 全生态的统一前端工作台与用户门户，整合所有 Fusion 系列产品的交互界面，提供统一账号体系、统一项目管理、统一数据看板、统一权限管控，是用户接触所有 Fusion 产品的唯一入口。
- **核心关系**：集成承载关系，Fusion Security 无独立前端产品，其 Web 控制台完全嵌入 fusion-studio 中，作为工作台的一个核心功能模块存在。
- **具体复用点**：
  1. 扫描任务管理、漏洞列表、补丁预览、安全报表、系统设置等所有前端页面，均以模块形式集成在 fusion-studio 内
  2. 复用统一账号、RBAC 权限、组织架构、项目空间、操作审计日志等通用管理能力，无需单独建设用户体系
  3. 与 fusion-code 及其他 Fusion 应用共享项目维度数据，同一项目空间内可同时使用多项能力
- **对标 Claude 生态**：等价于 claude.ai 官方网页工作台，Claude Security 作为功能模块集成在其中。

### 2.3 生态组件关系总览表

表格

| 组件名称 | 生态层级 | 核心定位 | 与 Fusion Security 的关系 | 依赖强度 | 对标 Claude 生态对应项 |
| --- | --- | --- | --- | --- | --- |
| fusion-mlx | 算力引擎层 | 本地大模型推理加速底座 | 所有 AI 推理的底层算力支撑 | 强依赖（必须） | Claude 模型推理引擎 |
| fusion-agent-studio | 核心底座层 | 智能体开发与运行平台 | 安全 Agent 的开发与运行底座 | 强依赖（必须） | Anthropic Agent SDK + 内部管线框架 |
| fusion-code | 垂直应用层 | 代码研发智能助手 | 平级兄弟产品，双向能力复用与场景集成 | 协同关系（非必须） | Claude Code |
| fusion-multi-node | 基础设施层 | 分布式多节点调度框架 | 企业级大规模部署的扩展支撑 | 可选依赖 | Anthropic 云端算力集群 |
| fusion-studio | 入口层 | 全生态统一工作台 | 前端界面承载与统一用户体系 | 集成承载 | claude.ai 官方工作台 |

---

## 三、整体技术架构

### 3.1 四层分层架构总览

Fusion Security 自身采用四层分层架构，向下对接 Fusion 生态底座能力，向上输出安全业务能力。

```
┌─────────────────────────────────────────────────────────┐
│                    应用接入层 (Access Layer)            │
│  Web Console  │  IDE Plugin  │  CLI  │  CI/CD  │  API  │
│ （承载于fusion-studio）                                │
├─────────────────────────────────────────────────────────┤
│                    能力服务层 (Service Layer)           │
│  代码扫描服务  │  漏洞验证服务  │  补丁生成服务  │      │
│  分诊分级服务  │  报告导出服务  │  工作流集成服务 │      │
├─────────────────────────────────────────────────────────┤
│                    引擎核心层 (Engine Layer)            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ 语义理解引擎 │  │ 数据流追踪  │  │ 对抗性验证  │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ 补丁生成引擎 │  │ 规则知识库  │  │ Agent编排器 │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│         （底层依赖fusion-mlx + fusion-agent-studio）    │
├─────────────────────────────────────────────────────────┤
│                  基础设施层 (Infra Layer)              │
│  本地部署  │  容器化部署  │  云端部署  │  离线模式    │
│  （分布式场景依赖fusion-multi-node）                    │
└─────────────────────────────────────────────────────────┘
```

### 3.2 核心模块详解

#### 3.2.1 语义理解引擎 (Semantic Understanding Engine)

- 代码抽象语法树解析：支持 20+ 主流编程语言的 AST 构建与语义分析
- 跨文件调用图构建：自动解析函数调用链、模块依赖关系、变量传递路径
- 框架语义识别：内置 Spring/Django/React/Express 等主流框架的安全规约知识
- 污点传播分析：追踪敏感输入（用户可控数据）到危险操作点的完整传播路径

#### 3.2.2 数据流追踪引擎 (Data Flow Tracker)

- 过程间分析：跨函数、跨文件、跨模块的正向 / 反向数据流追踪
- 污染标记系统：对输入源、清洗函数、危险汇点 (Sink) 进行三级标记
- 上下文敏感分析：区分不同调用上下文下的安全状态，减少误报
- 路径条件求解：结合控制流条件判断漏洞的可触达性

#### 3.2.3 对抗性验证引擎 (Adversarial Validator)

- 自质询机制：对初步发现的漏洞，自动生成反向论证尝试证伪
- 多视角交叉验证：分别从攻击者视角、防御者视角、业务逻辑视角三重校验
- 置信度评分：输出 0-100 分的漏洞置信度，低于阈值自动过滤
- 误报反馈闭环：人工标记的误报自动进入训练样本库，持续优化模型

#### 3.2.4 补丁生成引擎 (Patch Generator)

- 代码风格保持：学习项目现有编码风格，生成符合团队规范的修复代码
- 最小改动原则：仅修改必要代码行，避免引入额外变更风险
- 兼容性校验：自动检查补丁是否破坏现有 API 签名与业务逻辑
- 多方案备选：对高危漏洞提供 2-3 种修复方案并标注优劣对比

#### 3.2.5 Agent 编排器 (Agent Orchestrator)

- 五阶段标准管线：侦察 → 发现 → 分诊 → 报告 → 补丁，完整复刻安全研究员工作流
- 可插拔工具集：支持接入 Semgrep/CodeQL/Bandit 等传统扫描工具作为前置过滤器
- 自定义工作流：可视化编排安全检查流程，支持条件分支与并行执行
- 人工审核节点：关键操作强制引入人工决策，确保安全可控

### 3.3 技术栈选型

表格

| 层级 | 技术选型 | 说明 |
| --- | --- | --- |
| 模型推理 | MLX Framework + vLLM | Apple Silicon 原生加速，兼容 CUDA 后端 |
| 代码解析 | Tree-sitter + Code Embedding | 多语言统一解析框架 |
| Agent 框架 | Fusion-Agent Core | 复用 fusion-agent-studio 核心能力 |
| 数据存储 | PostgreSQL + Vector DB | 漏洞库 + 代码向量索引 |
| 前端 | React + Ant Design Pro | 集成于 fusion-studio |
| 部署 | Docker + Kubernetes + Helm | 容器化编排 |
| CI/CD 集成 | GitHub Actions / GitLab CI / Jenkins | 主流流水线插件 |
| IDE 插件 | VS Code Extension / JetBrains Plugin | 开发态实时扫描 |

### 3.4 跨组件调用链路（单次扫描任务）

1. **入口触发**：用户在 fusion-studio 发起代码扫描任务，或通过 CI/CD、IDE 插件触发
2. **任务调度**：请求转发至 fusion-agent-studio，进行任务注册、资源分配、权限校验
3. **代码预处理**：拉取代码仓库，执行 AST 解析、依赖分析，生成代码语义图谱
4. **AI 扫描执行**：调用 fusion-mlx 加载安全专项模型，执行语义扫描、数据流追踪，输出候选漏洞
5. **验证与分诊**：调度对抗性验证 Agent，对候选漏洞交叉校验，输出置信度与严重等级
6. **补丁生成**：调用 fusion-code 代码生成能力，生成符合项目风格的修复补丁
7. **结果回传**：所有结果汇总后存储，同步至 fusion-studio 前端展示，并推送至集成平台
8. **分布式场景**：超大规模任务通过 fusion-multi-node 拆分子任务，分发至多节点并行执行

---

## 四、核心功能模块

### 4.1 代码安全扫描

- **全量扫描**：对整个代码仓库执行深度安全审计
- **增量扫描**：仅扫描 PR/MR 变更代码，融入代码评审流程
- **定时扫描**：按计划执行周期性全量审计
- **定向扫描**：指定目录、文件或漏洞类型进行专项检查

**覆盖漏洞类型**：OWASP Top 10 全类别、业务逻辑漏洞（越权、竞态条件、身份验证绕过）、第三方依赖漏洞（SCA 集成）、配置安全缺陷、密码学误用、API 安全问题。

### 4.2 漏洞验证与分诊

- **置信度分级**：高 / 中 / 低三档置信度，优先处理高置信度漏洞
- **严重程度评级**：CVSS 3.1 标准评分，自动计算修复优先级
- **自动去重**：跨扫描任务识别重复漏洞，合并统计
- **误报一键标记**：支持人工标记误报，系统自动学习优化

### 4.3 自动修复与补丁生成

- **一键生成修复建议**：每条漏洞附带可直接应用的补丁代码
- **差异预览**：Side-by-side 展示修复前后代码对比
- **补丁应用模式**：支持自动创建修复分支、提交 PR
- **修复后复测**：补丁应用后自动重新扫描验证修复效果

### 4.4 安全报告与合规管理

- **多维度安全报表**：按项目、团队、漏洞类型、严重程度统计
- **趋势分析**：漏洞修复速率、新增趋势、技术债务变化
- **合规映射**：自动映射等保 2.0、ISO 27001、SOC 2 等合规要求
- **导出格式**：PDF/HTML/JSON/SARIF 标准格式

### 4.5 研发生态集成

- **代码托管平台**：GitHub / GitLab / Gitee / Bitbucket
- **项目管理**：Jira / 飞书 / 钉钉 / 企业微信
- **即时通讯**：Slack / 飞书群 / 钉钉群
- **SIEM 系统**：Splunk / ELK / 安全运营平台

### 4.6 企业级管理能力

- **多租户隔离**：组织 - 项目 - 用户三级权限体系
- **审计日志**：全操作可追溯，满足合规要求
- **自定义规则包**：企业专属安全规范与检查规则
- **模型管理**：本地模型上传、版本管理、灰度切换

---

## 五、标准工作流程

### 5.1 全链路扫描修复闭环

```
代码输入 → 预处理与解析 → 语义理解与数据流分析 → 初步漏洞发现
     ↓
人工审核 ← 补丁生成 ← 分诊分级 ← 对抗性自验证
     ↓
修复提交 → 回归验证 → 关闭漏洞
```

### 5.2 各阶段执行细节

**阶段一：侦察与预处理**

- 拉取代码仓库，识别项目类型与技术栈
- 构建依赖关系图，解析第三方组件清单
- 加载对应语言与框架的安全规则集

**阶段二：漏洞发现**

- 并行执行传统规则扫描（快速过滤已知模式）
- LLM 语义级深度扫描，跨文件追踪数据流
- 识别逻辑缺陷、权限问题、业务安全漏洞

**阶段三：对抗性验证**

- 对每个候选漏洞启动验证 Agent
- 尝试构造攻击路径证明漏洞真实存在
- 尝试从防御角度论证漏洞不可利用
- 综合输出置信度评分与严重等级

**阶段四：分诊与报告**

- 按严重程度、置信度、修复成本排序
- 生成结构化漏洞报告，包含成因、影响、复现步骤
- 推送到对应责任人与集成平台

**阶段五：补丁生成**

- 理解代码上下文与项目风格
- 生成符合编码规范的最小修复方案
- 自动校验语法正确性与基本逻辑
- 提供修复说明与注意事项

**阶段六：人工审核与闭环**

- 开发人员审核补丁代码
- 一键应用或修改后提交
- 自动触发回归扫描验证
- 漏洞状态流转至已修复

---

## 六、产品需求规格

### 6.1 产品目标

#### 6.1.1 业务目标

- 成为 Fusion-Agent Studio 生态核心安全组件
- 企业版首年获取 500+ 付费客户
- 开源版本 GitHub Star 突破 5k
- 在关键漏洞发现能力上达到 Claude Security 90% 水准

#### 6.1.2 性能目标

- 单万行代码扫描时间 < 5 分钟（本地 M3 Pro 机型）
- 高危漏洞检出率 > 95%（对比行业标准测试集）
- 误报率 < 10%（高置信度结果）
- 补丁语法正确率 > 98%

### 6.2 用户角色与核心需求

表格

| 用户角色 | 核心需求 |
| --- | --- |
| 安全工程师 | 深度漏洞发现、低误报、可定制规则、审计追溯 |
| 开发工程师 | IDE 内实时提示、一键修复、清晰的漏洞解释、不打断开发流程 |
| 技术负责人 | 全局安全态势、团队合规情况、修复效率统计 |
| DevOps 工程师 | CI/CD 无缝集成、可编程 API、可自动化流水线 |
| 运维管理员 | 私有化部署、高可用、易运维、资源可控 |

### 6.3 功能需求清单（FR）

#### 扫描引擎类

- FR-001：支持 Python/Java/JavaScript/TypeScript/Go/Rust/C/C++/PHP/Ruby 等至少 10 种主流语言
- FR-002：支持跨文件、跨目录的数据流追踪分析
- FR-003：支持识别主流 Web 框架的常见安全反模式
- FR-004：支持并行扫描，充分利用多核与多 GPU 资源
- FR-005：支持增量扫描，仅分析变更代码以提升效率

#### 漏洞验证类

- FR-006：每条漏洞必须附带置信度评分（0-100）
- FR-007：每条漏洞必须包含完整的数据流路径说明
- FR-008：支持人工标记误报并进入反馈闭环
- FR-009：支持 CVSS 3.1 自动评分与严重等级划分

#### 补丁生成类

- FR-010：高危与中危漏洞必须提供修复补丁建议
- FR-011：补丁必须通过语法正确性校验
- FR-012：支持生成 Git 格式 patch，可直接应用
- FR-013：补丁生成必须遵循项目现有编码风格

#### 集成与 API 类

- FR-014：提供 RESTful API 与 Webhook 机制
- FR-015：提供 GitHub Actions / GitLab CI 官方插件
- FR-016：提供 VS Code 与 JetBrains IDE 插件
- FR-017：支持 SARIF 标准格式导入导出

#### 管理与运维类

- FR-018：支持基于角色的访问控制 (RBAC)
- FR-019：支持完整的操作审计日志
- FR-020：支持单机与集群两种部署模式
- FR-021：支持离线环境部署与运行

### 6.4 非功能需求清单（NFR）

#### 安全与隐私

- NFR-001：本地部署模式下，所有代码与数据均不离开企业内网
- NFR-002：支持传输加密与静态存储加密
- NFR-003：所有敏感配置通过密钥管理系统存储
- NFR-004：自身代码需通过第三方安全审计

#### 性能与可扩展性

- NFR-005：支持水平扩展，扫描节点可动态增减
- NFR-006：单节点支持同时处理至少 5 个扫描任务
- NFR-007：任务队列支持优先级调度
- NFR-008：10 万行代码项目全量扫描 < 30 分钟

#### 可靠性

- NFR-009：系统可用性 ≥ 99.5%
- NFR-010：扫描任务支持断点续扫
- NFR-011：数据定期自动备份
- NFR-012：异常任务自动重试与熔断机制

#### 兼容性

- NFR-013：支持 macOS (Apple Silicon) / Linux / Windows 部署
- NFR-014：支持主流 Kubernetes 版本
- NFR-015：模型层兼容 OpenAI 兼容接口与本地模型

### 6.5 核心数据实体模型

- **Project（项目）**：代码仓库维度的扫描单元，关联代码仓库地址、技术栈、成员权限
- **Scan（扫描任务）**：单次扫描执行记录，包含扫描类型、状态、耗时、触发方式
- **Vulnerability（漏洞）**：发现的安全问题实体，包含类型、CVSS 评分、置信度、修复建议
- **Finding（发现项）**：漏洞在具体代码位置的实例，关联文件路径、行号、代码片段
- **Patch（补丁）**：漏洞对应的修复方案，包含补丁内容、方案说明、适用版本
- **Rule（规则）**：安全检查规则与策略，支持自定义规则包
- **User（用户）**：系统用户与权限，复用 fusion-studio 账号体系

---

## 七、版本迭代路线图

表格

| 版本 | 时间节点 | 核心里程碑 |
| --- | --- | --- |
| v0.1 Alpha | M1 | 核心扫描引擎 MVP，支持 Python/JS 两种语言，基础漏洞检测 |
| v0.5 Beta | M3 | 完整五阶段管线，对抗性验证上线，支持 8 种语言 |
| v1.0 GA | M6 | 正式发布，补丁生成能力完善，CI/CD 与 IDE 插件就绪 |
| v1.5 | M9 | 企业级多租户、审计合规、自定义规则引擎 |
| v2.0 | M12 | 高级 Agent 协作、实时运行时防护集成、威胁情报联动 |

---

## 八、差异化竞争优势

1. **极致隐私保护**：本地 MLX 推理，代码全程不出内网，解决 Claude Security 必须上传代码的核心痛点
2. **模型自由选择**：不绑定单一厂商模型，可根据成本与效果灵活切换，支持持续引入最新开源模型
3. **全链路可编程**：从扫描规则到验证逻辑再到补丁策略，全部支持深度定制，适配企业独特安全规范
4. **Fusion 生态协同**：与 fusion-mlx、fusion-agent-studio、fusion-code 无缝联动，构建完整 AI 研发安全体系
5. **成本优势显著**：开源版本免费使用，企业版订阅成本远低于 Claude Security 企业席位

---

需要我补充**核心 API 接口清单**或者**私有化部署实施方案**吗？

补充\*\*核心 API 接口清单\*\*或者\*\*私有化部署实施方案， 到同一篇文档

已完成思考

## 九、核心 API 接口清单

### 9.1 接口总体说明

Fusion Security 提供标准 RESTful API 接口，支持与企业现有 DevOps 流水线、安全运营平台、内部管理系统深度集成。所有接口遵循 OpenAPI 3.0 规范，采用 JSON 数据格式，支持 API Key 与签名鉴权两种认证方式，所有接口均支持私有化部署环境下的内网调用。

### 9.2 项目管理类 API

表格

| 接口名称 | 请求方法 | 接口路径 | 功能描述 | 核心入参 | 核心出参 |
| --- | --- | --- | --- | --- | --- |
| 创建项目 | POST | `/api/v1/projects` | 创建新的扫描项目，关联代码仓库与扫描规则配置 | project\_name、repo\_url、tech\_stack、default\_branch、ruleset\_id | project\_id、status、create\_time |
| 查询项目列表 | GET | `/api/v1/projects` | 分页查询当前权限范围内的所有项目 | page、page\_size、keyword、status | 项目列表、总数、分页信息 |
| 查询项目详情 | GET | `/api/v1/projects/{project_id}` | 获取单个项目的详细配置与统计信息 | 无（路径参数 project\_id） | 项目基础信息、扫描统计、配置详情 |
| 更新项目配置 | PUT | `/api/v1/projects/{project_id}` | 修改项目的扫描规则、仓库分支、成员权限等配置 | 配置字段集合 | 更新后的项目详情 |
| 删除项目 | DELETE | `/api/v1/projects/{project_id}` | 删除指定项目及关联的历史扫描数据 | 无（路径参数 project\_id） | 操作状态码 |

### 9.3 扫描任务类 API

表格

| 接口名称 | 请求方法 | 接口路径 | 功能描述 | 核心入参 | 核心出参 |
| --- | --- | --- | --- | --- | --- |
| 触发全量扫描 | POST | `/api/v1/scans` | 对指定项目启动全量代码安全扫描 | project\_id、branch、ruleset\_id、priority | scan\_id、status、start\_time |
| 触发增量扫描 | POST | `/api/v1/scans/incremental` | 仅扫描指定 commit 区间的变更代码 | project\_id、base\_commit、head\_commit | scan\_id、status、start\_time |
| 查询扫描状态 | GET | `/api/v1/scans/{scan_id}` | 获取单个扫描任务的执行进度与状态 | 无（路径参数 scan\_id） | 任务状态、进度百分比、耗时、统计概览 |
| 取消扫描任务 | DELETE | `/api/v1/scans/{scan_id}` | 终止正在运行的扫描任务 | 无（路径参数 scan\_id） | 操作状态码 |
| 查询扫描列表 | GET | `/api/v1/scans` | 分页查询项目下的历史扫描记录 | project\_id、page、page\_size、status | 扫描记录列表、总数、分页信息 |

### 9.4 漏洞与发现项类 API

表格

| 接口名称 | 请求方法 | 接口路径 | 功能描述 | 核心入参 | 核心出参 |
| --- | --- | --- | --- | --- | --- |
| 查询漏洞列表 | GET | `/api/v1/vulnerabilities` | 按条件筛选查询漏洞清单 | scan\_id、project\_id、severity、confidence、status | 漏洞列表、统计汇总、分页信息 |
| 查询漏洞详情 | GET | `/api/v1/vulnerabilities/{vuln_id}` | 获取单条漏洞的完整信息、数据流路径与复现说明 | 无（路径参数 vuln\_id） | 漏洞详情、影响范围、修复建议、发现项列表 |
| 更新漏洞状态 | PUT | `/api/v1/vulnerabilities/{vuln_id}/status` | 修改漏洞处理状态（待处理 / 修复中 / 已修复 / 已忽略） | status、remark | 更新后的漏洞状态 |
| 标记误报 | POST | `/api/v1/vulnerabilities/{vuln_id}/false-positive` | 标记漏洞为误报并提交反馈，进入模型优化闭环 | reason | 操作状态码 |
| 查询发现项详情 | GET | `/api/v1/findings/{finding_id}` | 获取漏洞在具体代码位置的实例详情与代码片段 | 无（路径参数 finding\_id） | 文件路径、行号、代码片段、上下文信息 |

### 9.5 补丁修复类 API

表格

| 接口名称 | 请求方法 | 接口路径 | 功能描述 | 核心入参 | 核心出参 |
| --- | --- | --- | --- | --- | --- |
| 生成修复补丁 | POST | `/api/v1/patches/generate` | 为指定漏洞生成修复补丁方案 | vuln\_id、patch\_strategy | patch\_id、生成状态、补丁概览 |
| 查询补丁详情 | GET | `/api/v1/patches/{patch_id}` | 获取补丁的完整代码差异与修复说明 | 无（路径参数 patch\_id） | diff 内容、修复说明、风险提示 |
| 创建修复分支 | POST | `/api/v1/patches/{patch_id}/apply` | 自动创建修复分支并提交补丁代码 | branch\_name、commit\_message | 分支地址、PR 链接（若配置） |
| 补丁有效性验证 | POST | `/api/v1/patches/{patch_id}/verify` | 应用补丁后执行回归扫描，验证漏洞是否修复 | 无 | 验证结果、是否修复成功、残留风险 |

### 9.6 报告与导出类 API

表格

| 接口名称 | 请求方法 | 接口路径 | 功能描述 | 核心入参 | 核心出参 |
| --- | --- | --- | --- | --- | --- |
| 生成扫描报告 | POST | `/api/v1/reports` | 生成指定扫描任务的完整安全报告 | scan\_id、template、format | report\_id、生成状态 |
| 查询报告状态 | GET | `/api/v1/reports/{report_id}` | 查询报告生成进度与下载地址 | 无（路径参数 report\_id） | 报告状态、下载链接、有效期 |
| 导出漏洞数据 | GET | `/api/v1/vulnerabilities/export` | 按条件导出漏洞清单数据 | project\_id、format、filter 条件 | 导出文件下载链接 |
| SARIF 格式导出 | GET | `/api/v1/scans/{scan_id}/sarif` | 导出标准 SARIF 格式的扫描结果，用于第三方平台对接 | 无（路径参数 scan\_id） | SARIF 格式 JSON 数据 |

### 9.7 集成与回调类 API

表格

| 接口名称 | 请求方法 | 接口路径 | 功能描述 | 核心入参 | 核心出参 |
| --- | --- | --- | --- | --- | --- |
| Webhook 配置 | POST | `/api/v1/webhooks` | 配置事件回调地址，支持扫描完成、漏洞新增等事件触发 | url、events、secret | webhook\_id、配置信息 |
| 第三方结果接入 | POST | `/api/v1/integrations/findings` | 接入外部 SAST/DAST 工具的扫描结果，统一汇总管理 | source、findings 数组 | 接入数量、去重结果 |
| 流水线准入校验 | POST | `/api/v1/gates/check` | CI/CD 流水线门禁校验，判断当前代码是否满足安全准入标准 | project\_id、branch、threshold | 校验结果、阻断原因、通过状态 |

### 9.8 系统管理类 API

表格

| 接口名称 | 请求方法 | 接口路径 | 功能描述 | 核心入参 | 核心出参 |
| --- | --- | --- | --- | --- | --- |
| 模型配置管理 | PUT | `/api/v1/system/models` | 配置本地模型路径、量化参数、推理并发数 | model\_config | 更新后的配置 |
| 规则包管理 | POST | `/api/v1/rulesets` | 上传与管理自定义安全规则包 | name、rules、scope | ruleset\_id、规则数量 |
| 系统健康检查 | GET | `/api/v1/system/health` | 查询系统运行状态、组件健康度、资源使用率 | 无 | 各组件状态、资源使用率、告警信息 |

---

## 十、私有化部署实施方案

### 10.1 部署核心原则

1. **数据不出域原则**：所有代码扫描、模型推理、漏洞分析全流程在企业内网完成，敏感数据零外泄
2. **平滑演进原则**：支持从单机 PoC 到集群部署的无缝升级，保护前期投入
3. **开箱即用原则**：提供标准化容器镜像与一键部署脚本，降低运维门槛
4. **合规可审计原则**：全链路操作可追溯、数据可加密，满足等保 2.0、ISO 27001 等合规要求

### 10.2 部署模式选型

#### 10.2.1 单机私有化部署

- **适用场景**：10 人以内安全团队、中小规模研发团队、产品 PoC 验证、个人开发者
- **依赖组件**：fusion-mlx + fusion-agent-studio + fusion-studio + Fusion Security
- **架构特点**：所有组件部署在单台服务器 / 工作站上，资源占用低、部署周期短、运维成本低
- **典型拓扑**：单节点承载推理算力、Agent 调度、前端门户、数据存储全部能力

#### 10.2.2 分布式集群部署

- **适用场景**：中大型企业、多项目并发扫描、高可用要求高、代码规模百万行以上
- **依赖组件**：全部基础组件 + fusion-multi-node
- **架构特点**：控制节点与工作节点分离，扫描能力可水平扩展；支持多项目并行扫描，任务自动负载均衡
- **典型拓扑**：控制节点（管理 + 调度）+ 若干扫描工作节点 + 独立存储集群

#### 10.2.3 完全离线部署

- **适用场景**：军工、金融、政务等强保密单位、物理隔离内网环境、代码严禁出域
- **依赖组件**：全量离线安装包，无任何外部网络依赖
- **架构特点**：全链路离线运行，模型、规则包、依赖组件均支持离线导入升级；禁用所有外发请求与遥测
- **合规优势**：满足等保三级、涉密信息系统等强安全合规要求

### 10.3 硬件与环境要求

#### 10.3.1 单机部署配置建议

表格

| 平台 | 最低配置 | 推荐配置 | 适用规模 |
| --- | --- | --- | --- |
| macOS Apple Silicon | M2 Pro / 16GB 统一内存 / 100GB SSD | M3 Max / 32GB 统一内存 / 200GB SSD | 10 万行以内代码，日均扫描 ≤ 5 次 |
| Linux x86/ARM | 16 核 CPU / 32GB 内存 / 24GB 显存 GPU / 200GB SSD | 32 核 CPU / 64GB 内存 / 48GB 显存 GPU / 500GB SSD | 50 万行以内代码，日均扫描 ≤ 20 次 |
| 操作系统要求 | macOS 14+ / Ubuntu 22.04+ / CentOS Stream 8+ | 同左 | - |

#### 10.3.2 集群部署配置建议

表格

| 节点角色 | 配置要求 | 数量建议 | 核心职责 |
| --- | --- | --- | --- |
| 控制节点 | 16 核 CPU / 32GB 内存 / 500GB SSD | 主备 2 台（高可用） | 任务调度、用户管理、前端门户、数据存储 |
| 扫描工作节点 | 16 核 CPU / 32GB 内存 / 24GB 显存 GPU / 200GB SSD | 按需扩容，起步 2 台 | 代码扫描、模型推理、补丁生成 |
| 存储节点 | PostgreSQL 主从 + 向量数据库 / 1TB SSD | 主备 2 台 | 漏洞库、项目配置、审计日志持久化 |

### 10.4 标准部署流程

#### 步骤 1：环境准备与前置检查

- 服务器操作系统初始化，关闭不必要的端口与服务
- 安装 Docker / Kubernetes 容器运行环境
- 配置内网镜像仓库，同步所需基础镜像
- 检查硬件资源与网络连通性，确认各节点间通信正常

#### 步骤 2：生态底座组件部署

1. **部署 fusion-mlx**：加载本地大模型文件，配置量化参数与并发数，验证推理接口可用性
2. **部署 fusion-agent-studio**：配置 Agent 运行环境，注册基础工具集，验证工作流调度能力
3. **部署 fusion-studio**：初始化统一门户，配置账号体系与 RBAC 权限，验证前端访问正常

#### 步骤 3：Fusion Security 应用部署

- 部署 Fusion Security 服务模块，注册到底座组件
- 导入默认安全规则包与漏洞知识库
- 配置代码仓库访问凭证与集成密钥
- 完成与 fusion-studio 前端模块的挂载集成

#### 步骤 4：系统初始化与验证

- 创建管理员账号，配置组织架构与项目权限
- 执行示例项目冒烟扫描，验证全链路功能正常
- 校验漏洞检出、补丁生成、报告导出核心能力
- 配置监控告警与日志归集

### 10.5 高可用与容灾方案

- **控制节点高可用**：采用主备双节点部署，通过 Keepalived 实现 VIP 漂移，故障自动切换，切换时间 < 30s
- **任务调度容灾**：扫描任务全量持久化存储，工作节点故障时，任务自动转移至健康节点重试，不丢失扫描进度
- **数据备份机制**：漏洞库、项目配置、审计日志每日全量备份 + 实时增量备份，支持一键恢复；支持异地容灾同步
- **降级运行机制**：多节点故障时，系统自动降级为单机模式，保障核心扫描与查询能力可用，非核心功能暂时关闭

### 10.6 运维与监控体系

#### 10.6.1 监控指标

- **系统层监控**：CPU / 内存 / GPU 使用率、磁盘空间、网络带宽、节点健康状态
- **业务层监控**：扫描任务成功率、平均扫描时长、漏洞检出量、误报率、修复闭环率
- **模型层监控**：推理吞吐量、平均响应时延、显存占用、模型加载状态

#### 10.6.2 运维能力

- 提供可视化运维控制台，支持节点管理、任务监控、告警配置
- 支持灰度升级与版本回滚，升级过程不中断业务运行
- 内置日志归集系统，全链路运行日志、操作日志统一查询与导出
- 提供标准运维 API，支持接入企业现有运维平台

### 10.7 数据安全保障

1. **传输安全**：全链路 HTTPS/TLS 1.3 加密，内部服务间采用双向 TLS 认证，防止中间人攻击
2. **存储安全**：代码缓存、漏洞数据、报告文件全部静态加密存储；敏感配置通过密钥管理系统统一托管
3. **权限管控**：细粒度 RBAC 权限体系，代码访问权限、扫描权限、管理权限分离；支持操作二次认证
4. **审计追溯**：所有用户操作、扫描行为、数据变更全量审计日志，留存周期不少于 180 天，支持合规审计
5. **代码隐私**：本地扫描模式下，代码仅在内存中加载分析，扫描完成后可配置自动清理临时缓存，不持久化存储完整代码

---

需要我再补充**漏洞规则库分类清单**或者**与主流 DevOps 工具的集成配置手册**吗？

补充\*\*漏洞规则库分类清单\*\*或者\*\*与主流 DevOps 工具的集成配置手册\*\*在同一篇文档中

已完成思考

## 十一、漏洞规则库分类清单

### 11.1 规则库总体说明

Fusion Security 规则库采用「**基础规则层 + 语义规则层 + 自定义规则层**」三级架构，兼顾扫描效率与检测深度：

- **基础规则层**：基于 AST 语法树与正则匹配的已知漏洞模式，扫描速度快、误报可控，覆盖绝大多数通用已知漏洞
- **语义规则层**：基于大模型语义理解与跨文件数据流追踪，识别传统规则无法覆盖的业务逻辑漏洞、复杂利用链漏洞
- **自定义规则层**：支持企业基于自身安全规范上传专属规则，支持正则、AST、自然语言描述三种规则定义方式

规则库全面对齐 OWASP Top 10、CWE 通用缺陷枚举、等保 2.0、ISO 27001 等标准体系，所有规则均附带唯一编号、严重等级、修复指引与参考标准，支持按项目维度灵活启停。

**严重等级划分标准**：

- 高危：可直接导致服务器被接管、数据泄露、权限绕过，需立即修复
- 中危：可导致有限数据泄露、普通权限提升、局部服务异常，需计划修复
- 低危：信息泄露、配置不规范，利用难度高，影响范围有限
- 信息：代码风格、最佳实践建议，无直接安全风险

### 11.2 官方规则库分类明细

#### 11.2.1 注入类漏洞（对应 OWASP A03: Injection）

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| SQL 注入 | FUS-INJ-001 | 高危 | 数据流追踪 + AST 规则 | 全语言 | 检测用户可控输入未经过滤直接拼接 SQL 语句的场景，支持跨函数、跨文件传播链识别 |
| 操作系统命令注入 | FUS-INJ-002 | 高危 | 数据流追踪 + AST 规则 | 全语言 | 检测用户输入直接传入系统命令执行函数，包含命令拼接、参数注入两类场景 |
| 跨站脚本攻击 (XSS) | FUS-INJ-003 | 中危 / 高危 | 数据流追踪 | 前端 + 后端语言 | 覆盖反射型、存储型、DOM 型三类 XSS，识别输出编码缺失场景 |
| XML 外部实体注入 (XXE) | FUS-INJ-004 | 高危 | AST 规则 + 语义识别 | Java/PHP/Python | 检测 XML 解析器未禁用外部实体配置，可导致文件读取、SSRF 等风险 |
| 表达式注入 | FUS-INJ-005 | 高危 | AST 规则 + 数据流追踪 | Java/JS/Python | 检测 SpEL、OGNL、eval 等危险表达式执行用户可控输入 |
| LDAP 注入 | FUS-INJ-006 | 中危 | AST 规则 | 全语言 | 检测 LDAP 查询语句拼接用户输入，可导致权限绕过与数据泄露 |
| 模板注入 | FUS-INJ-007 | 高危 | 语义识别 + 数据流追踪 | 全语言 | 检测服务端模板引擎接收用户可控模板内容，可导致远程代码执行 |

#### 11.2.2 身份认证与会话管理（对应 OWASP A07: Identification and Authentication Failures）

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 硬编码凭证 | FUS-AUTH-001 | 高危 | 正则 + 语义识别 | 全语言 | 检测代码中硬编码的密码、Token、密钥、Access Key 等敏感凭证 |
| 弱密码策略 | FUS-AUTH-002 | 中危 | 语义识别 | 全语言 | 检测用户注册 / 密码修改接口未设置密码复杂度要求 |
| JWT 配置缺陷 | FUS-AUTH-003 | 高危 | AST 规则 + 语义识别 | 全语言 | 检测 JWT 签名校验禁用、弱密钥、算法可被篡改等配置问题 |
| 会话固定攻击 | FUS-AUTH-004 | 中危 | 语义识别 | 全语言 | 检测登录成功后未重新生成会话 ID，存在会话固定风险 |
| 验证码缺失 / 可复用 | FUS-AUTH-005 | 中危 | AI 语义分析 | 全语言 | 识别登录、注册、短信发送等接口未配置验证码或验证码未一次性失效 |
| 多因素认证缺失 | FUS-AUTH-006 | 低危 | 语义识别 | 全语言 | 管理员后台、核心操作接口未强制多因素认证 |

#### 11.2.3 访问控制漏洞（对应 OWASP A01: Broken Access Control）

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 水平越权访问 | FUS-ACL-001 | 高危 | AI 语义分析 + 数据流追踪 | 全语言 | 识别接口未校验数据归属权，用户可访问他人数据的场景 |
| 垂直越权访问 | FUS-ACL-002 | 高危 | AI 语义分析 | 全语言 | 识别普通用户可访问管理员权限接口，权限校验缺失 |
| 路径遍历漏洞 | FUS-ACL-003 | 高危 | 数据流追踪 + AST 规则 | 全语言 | 检测文件读取 / 下载接口未过滤../ 等路径穿越字符 |
| 不安全的文件上传 | FUS-ACL-004 | 高危 | 语义识别 | 全语言 | 检测文件上传功能未校验文件类型、后缀、内容，可上传恶意脚本 |
| 接口权限绕过 | FUS-ACL-005 | 中危 | AST 规则 | 全语言 | 识别鉴权中间件存在绕过路径，或部分接口未纳入鉴权范围 |

#### 11.2.4 敏感数据保护（对应 OWASP A02: Cryptographic Failures）

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 敏感数据明文存储 | FUS-DATA-001 | 高危 | 语义识别 | 全语言 | 检测密码、身份证、银行卡等敏感信息明文存入数据库 |
| 日志泄露敏感信息 | FUS-DATA-002 | 中危 | 数据流追踪 | 全语言 | 检测日志输出中包含密码、Token、用户隐私等敏感数据 |
| 接口返回敏感信息 | FUS-DATA-003 | 中危 | 语义识别 | 全语言 | 检测接口响应中返回多余敏感字段，前端未展示但可被抓包获取 |
| 不安全的数据传输 | FUS-DATA-004 | 中危 | 配置扫描 | 全语言 | 检测接口未强制 HTTPS，或存在 HTTP 明文传输敏感数据 |

#### 11.2.5 密码学误用

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 弱加密算法 | FUS-CRYPTO-001 | 高危 | AST 规则 | 全语言 | 检测使用 MD5、SHA1、DES、RC4 等已被破解的弱加密 / 哈希算法 |
| 硬编码加密密钥 | FUS-CRYPTO-002 | 高危 | 正则 + 语义识别 | 全语言 | 检测加密密钥直接写在代码或配置文件中 |
| 不安全的随机数 | FUS-CRYPTO-003 | 中危 | AST 规则 | 全语言 | 检测使用伪随机数生成器生成验证码、Token、密钥等安全敏感数据 |
| SSL 证书校验禁用 | FUS-CRYPTO-004 | 高危 | AST 规则 | 全语言 | 检测 HTTPS 请求时禁用服务端证书校验，存在中间人攻击风险 |

#### 11.2.6 安全配置错误（对应 OWASP A05: Security Misconfiguration）

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 默认账号密码 | FUS-CONF-001 | 高危 | 配置扫描 | 全语言 | 检测框架、组件、数据库使用默认账号密码 |
| 详细错误信息泄露 | FUS-CONF-002 | 中危 | 语义识别 | 全语言 | 检测生产环境开启调试模式，错误页面返回堆栈、路径等敏感信息 |
| CORS 配置过宽 | FUS-CONF-003 | 中危 | 配置扫描 + 语义识别 | 全语言 | 检测跨域配置允许任意来源，存在 CSRF 与数据泄露风险 |
| 不安全的 HTTP 响应头 | FUS-CONF-004 | 低危 | 配置扫描 | 全语言 | 缺失 X-Frame-Options、X-Content-Type-Options、CSP 等安全响应头 |
| 目录遍历开放 | FUS-CONF-005 | 中危 | 配置扫描 | Nginx/Apache | Web 服务器开启目录列表，可遍历下载源码与资源文件 |

#### 11.2.7 业务逻辑漏洞（AI 语义检测专属）

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 竞态条件漏洞 | FUS-LOGIC-001 | 高危 | AI 语义分析 + 数据流追踪 | 全语言 | 识别扣减库存、余额扣款等场景存在并发竞态，可导致超扣、重复下单 |
| 支付逻辑漏洞 | FUS-LOGIC-002 | 高危 | AI 语义分析 | 全语言 | 识别支付金额可被前端篡改、支付状态可被伪造等逻辑缺陷 |
| 短信 / 邮箱轰炸 | FUS-LOGIC-003 | 中危 | AI 语义分析 | 全语言 | 识别发送验证码接口无频率限制、无校验机制，可被滥用轰炸 |
| 接口重放风险 | FUS-LOGIC-004 | 中危 | AI 语义分析 | 全语言 | 核心操作接口无防重放机制，请求可被重复执行 |

#### 11.2.8 第三方依赖与组件安全（SCA）

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 已知 CVE 漏洞组件 | FUS-SCA-001 | 高危 / 中危 | 组件指纹匹配 | 全语言 | 比对依赖版本与 NVD 漏洞库，识别存在已知高危漏洞的第三方组件 |
| 已废弃组件 | FUS-SCA-002 | 中危 | 组件库比对 | 全语言 | 识别长期停止维护、已官方废弃的开源组件 |
| 许可证合规风险 | FUS-SCA-003 | 低危 / 中危 | 许可证识别 | 全语言 | 检测 GPL 等强传染许可证组件，识别商业软件合规风险 |
| 组件版本过旧 | FUS-SCA-004 | 低危 | 版本比对 | 全语言 | 依赖组件版本落后主分支多个大版本，存在潜在安全与兼容性风险 |

#### 11.2.9 不安全的反序列化与其他高危漏洞

表格

| 规则名称 | 规则编号 | 严重等级 | 检测方式 | 覆盖语言 | 规则说明 |
| --- | --- | --- | --- | --- | --- |
| 不安全的反序列化 | FUS-DESER-001 | 高危 | AST 规则 + 语义识别 | Java/Python/PHP | 检测反序列化用户可控数据，可导致远程代码执行 |
| SSRF 服务端请求伪造 | FUS-SSRF-001 | 高危 | 数据流追踪 | 全语言 | 检测服务端请求用户可控 URL，可探测内网、读取本地文件 |
| CSRF 跨站请求伪造 | FUS-CSRF-001 | 中危 | 语义识别 | 全语言 | 核心状态变更接口未配置 CSRF Token 校验 |

### 11.3 规则库管理机制

1. **版本迭代**：官方规则库每月迭代更新，同步新增 CVE 漏洞、新兴攻击手法、行业最佳实践
2. **自定义规则**：企业可通过自然语言描述新增专属业务安全规则，系统自动转化为语义检测逻辑
3. **规则灰度**：支持规则按项目灰度启用，先观察后正式接入，避免误报影响研发流程
4. **误报闭环**：人工标记的误报自动进入规则优化样本库，定期迭代优化检测准确率

---

## 十二、主流 DevOps 工具集成配置手册

### 12.1 集成总体说明

Fusion Security 遵循 DevSecOps 左移理念，提供标准化插件与 API，可无缝嵌入研发全流程，实现「编码时实时提示、提交时增量扫描、合并前门禁校验、上线后定期审计」的全链路安全防护。所有集成均支持私有化部署环境内网运行，无需访问外部服务。

### 12.2 GitHub Actions 集成配置

#### 集成能力

- PR 提交时自动触发增量扫描，仅扫描变更代码
- 扫描结果自动评论回写到 PR 页面，标注漏洞位置与修复建议
- 支持安全门禁配置，高危漏洞未修复时阻断合并
- 支持 SARIF 格式结果上传至 GitHub Security 面板

#### 配置步骤

1. 在项目仓库中创建 `.github/workflows/fusion-security.yml` 配置文件
2. 在仓库 Secrets 中配置 `FUSION_SECURITY_API_KEY` 与 `FUSION_SECURITY_ENDPOINT`
3. 配置门禁阈值与触发规则，提交后生效

#### 配置示例

```
name: Fusion Security 代码安全扫描
on:
  pull_request:
    branches: [ main, develop ]  # PR 合并到目标分支时触发

jobs:
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 拉取完整提交历史用于增量对比

      - name: 执行 Fusion Security 增量扫描
        uses: fusion-security/scan-action@v1
        with:
          api-endpoint: ${{ secrets.FUSION_SECURITY_ENDPOINT }}
          api-key: ${{ secrets.FUSION_SECURITY_API_KEY }}
          project-id: "your-project-id"  # Fusion Security 中的项目ID
          scan-mode: incremental  # 增量扫描模式
          severity-gate: "high"  # 存在高危漏洞则阻断
          fail-on-error: true  # 不满足门禁时流水线失败

      - name: 上传扫描结果到 GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: fusion-security-result.sarif
```

### 12.3 GitLab CI/CD 集成配置

#### 集成能力

- 代码提交、MR 创建时自动触发扫描
- 扫描结果集成到 GitLab 代码质量报告
- 支持流水线门禁，不满足安全要求时阻断合并
- 支持自托管 GitLab 内网部署

#### 配置示例 `.gitlab-ci.yml`

```
stages:
  - test
  - security

fusion_security_scan:
  stage: security
  image: fusion-security/cli:latest
  variables:
    FUSION_ENDPOINT: $FUSION_SECURITY_ENDPOINT
    FUSION_API_KEY: $FUSION_SECURITY_API_KEY
    PROJECT_ID: "your-project-id"
  script:
    # 执行增量扫描，仅扫描当前提交与目标分支的差异
    - fusion-cli scan incremental 
        --project-id $PROJECT_ID 
        --base-commit $CI_MERGE_REQUEST_DIFF_BASE_SHA 
        --head-commit $CI_COMMIT_SHA
    # 门禁校验：高危漏洞数 > 0 则退出非零状态码
    - fusion-cli gate check --severity high --fail-on-violation
  artifacts:
    reports:
      sarif: fusion-result.sarif  # 接入 GitLab 安全面板
    paths:
      - fusion-security-report.html
    when: always
  only:
    - merge_requests
    - main
    - develop
```

### 12.4 Jenkins 集成配置

#### 集成能力

- 支持自由风格项目与流水线项目集成
- 可配置代码提交触发、定时全量扫描多种模式
- 扫描结果生成可视化报告归档
- 支持与 Jenkins 质量门禁插件联动

#### 配置示例 Jenkinsfile (声明式流水线)

```
pipeline {
    agent any
    environment {
        FUSION_ENDPOINT = credentials('fusion-security-endpoint')
        FUSION_API_KEY = credentials('fusion-security-api-key')
    }
    stages {
        stage('代码检出') {
            steps {
                git branch: 'main', url: 'https://your-git-repo.git'
            }
        }
        stage('安全扫描') {
            steps {
                sh '''
                    fusion-cli scan full \
                      --project-id "your-project-id" \
                      --output sarif \
                      --output-file result.sarif
                '''
            }
            post {
                always {
                    // 归档扫描报告
                    archiveArtifacts artifacts: 'result.sarif, fusion-report.html', allowEmptyArchive: true
                    // 质量门禁校验
                    sh 'fusion-cli gate check --severity high --max-count 0'
                }
            }
        }
    }
}
```

### 12.5 飞书 / 钉钉 消息通知集成

#### 集成能力

- 扫描完成后推送结果通知到指定群聊
- 高危漏洞实时告警，@对应负责人
- 支持每日 / 每周安全日报自动推送
- 点击通知可直接跳转至漏洞详情页

#### 配置步骤

1. 在飞书 / 钉钉群中创建自定义机器人，获取 Webhook 地址
2. 进入 Fusion Security 控制台 → 集成中心 → 消息通知
3. 新建通知规则，选择触发事件（扫描完成、高危漏洞新增、每日日报）
4. 填入 Webhook 地址与签名密钥，配置通知模板与 @人员
5. 保存后发送测试消息验证连通性

### 12.6 Jira 工单自动同步集成

#### 集成能力

- 新发现高危 / 中危漏洞自动创建 Jira 工单
- 漏洞与工单状态双向同步，漏洞修复后自动关闭对应工单
- 支持按漏洞等级分配到不同处理人
- 支持自定义工单字段与流程

#### 配置步骤

1. 在 Jira 中创建 API 访问令牌，拥有工单创建与编辑权限
2. 进入 Fusion Security 控制台 → 集成中心 → Jira 集成
3. 填入 Jira 站点地址、账号、API Token，验证连通性
4. 配置同步规则：选择项目、漏洞等级范围、工单类型、默认处理人
5. 配置字段映射，将漏洞属性对应到 Jira 工单字段
6. 开启同步，测试创建一条漏洞验证工单生成正常

### 12.7 IDE 插件集成（VS Code / JetBrains）

#### 集成能力

- 编码过程中实时检测安全漏洞，编辑器内悬浮提示
- 支持一键生成修复补丁，直接应用到代码
- 支持对选中代码片段执行专项安全检测
- 漏洞详情附带原理说明与修复指导，边开发边学习安全规范

#### VS Code 配置步骤

1. 在 VS Code 插件市场搜索「Fusion Security」并安装
2. 打开插件设置，配置私有化部署服务地址与 API Key
3. 配置扫描触发时机：保存时扫描 / 实时扫描 / 手动触发
4. 打开代码文件后自动生效，漏洞行号处会显示标记，悬浮查看详情
5. 点击「一键修复」按钮，自动生成补丁并预览差异后应用
